version:  '3'
services:
    nginx:
      depends_on:
        - wordpress
      container_name: nginx
      build: requirements/nginx/
      volumes:
        - "~/data/wordpress:/var/www/html"
      ports:
        - "443:443"
      networks:
        - network
      restart: always
    wordpress:
      depends_on:
        - mariadb
      container_name: wordpress
      build: ./requirements/wordpress
      env_file: .env
      networks:
        - network
      volumes:
        - "~/data/wordpress:/var/www/html"
      restart: always
    mariadb:
          container_name: mariadb
          env_file: .env
          build: ./requirements/mariadb
          networks:
            - network
          volumes:
            - "~/data/mariadb:/var/www/html"
          restart: always
networks:
  network:
    driver: bridge
